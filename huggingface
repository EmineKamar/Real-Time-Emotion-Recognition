#huggingface
from datasets import load_dataset

crema = load_dataset("myleslinder/crema-d", trust_remote_code=True)

from datasets import load_dataset

dataset = load_dataset("myleslinder/crema-d")
sample = dataset["train"][0]
print(sample.keys())
print(sample)
